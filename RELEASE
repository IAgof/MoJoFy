v0.13.1
- Fix dynamo driver upsert get callback that caused not merging new data with existing data
- Update dynamo driver upsert to set data.creation_date and data.modification_date
- Fix query function logger.debug call that modified the content of params and caused never calling list
- Fix table already exist errors in createTable, getting existing remote tables